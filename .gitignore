*.o
*.elf
*.hex
build/

# Editor / tooling
.claude/

# Dev-only files -- kept local, excluded from the published library
# (DEVELOPMENT.md stays tracked: README links to it, so it must ship)
test/
.audit/
